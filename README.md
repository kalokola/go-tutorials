journey to gin-gonic
we base on ::
https://go.dev/doc/tutorial/web-service-gin


go mod init example/web-service-gin


use `go get .` to install defined packages
`go mod tidy` removes unused depedencies,adds what required and saved checksums (security verifications)
`go mod download` after cloning a package 