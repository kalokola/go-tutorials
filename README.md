hints
- use `go run .` if you have different files linking (imports)
- `go run file-name.go` just runs a single file, so it will throw errors


`fmt.Println()` - print a new line after output
`fmt.Print()` - print a raw format